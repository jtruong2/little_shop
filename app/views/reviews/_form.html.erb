<%= form_for [ @item, @review ] do |f| %>
  <p>
    <%= f.label :body, "Your Review" %><br/>
    <%= f.text_area :body %>
  </p>

  <%= hidden_field_tag :item_id, @item.id %>

  <p>
    <%= f.submit 'Submit' %>
  </p>
<% end %>
