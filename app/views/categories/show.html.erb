<div id="categoryshow" class="jumbotron">
  <div class="container">
    <% @items.each do |item|  %>
      <%=  link_to item.title, item_path(item) %><br>
      <%= link_to image_tag(item.image), item_path(item) %>   <em>Price:</em> <%= item.price %><br><br>
    <% end %>
  </div>
</div>
