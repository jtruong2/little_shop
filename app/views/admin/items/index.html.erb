<h1>Admin Items</h1>
<% @items.each do |item| %>
  <%= item.title %>
  <%= item.description %>
  <%= item.price %>
  <%= item.category.name %>
  <%= item.image %>
  <%= link_to "Edit", edit_admin_item_path(item) %>
<% end %>

